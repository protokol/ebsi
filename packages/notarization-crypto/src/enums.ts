import { defaults } from "./defaults";

export enum NotarizationTransactionTypes {
    Notarization = 0,
}

export const NotarizationTransactionGroup = defaults.notarizationTypeGroup;

export enum NotarizationStaticFees {
    Notarization = "500000000",
}
